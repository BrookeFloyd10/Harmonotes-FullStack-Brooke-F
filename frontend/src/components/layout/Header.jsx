import NavBar from "./NavBar";

const Header = () => {
    return (
        <header className="site-header">
            <div>
                <h1>Harmonotes</h1>
            </div>

            <NavBar />
        </header>
    );
}

export default Header;